import { SettingsClient } from "@/components/features/settings/SettingsClient";
export const metadata = { title: "Settings — VaccinationBD Centers" };
export default function WorkerSettingsPage() { return <SettingsClient />; }
