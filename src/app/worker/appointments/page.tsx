import { AppointmentsClient } from "@/components/features/appointments/AppointmentsClient";

export const metadata = { title: "Appointments — VaccinationBD Centers" };

export default function WorkerAppointmentsPage() {
  return <AppointmentsClient />;
}
