import { AuditClient } from "@/components/features/audit/AuditClient";

export const metadata = { title: "Security & Audit — VaccinationBD Centers" };

export default function WorkerAuditPage() {
  return <AuditClient />;
}
